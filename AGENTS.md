# AGENTS.md

## Cursor Cloud specific instructions

This is a documentation-only repository (`try-cursor-pub`). It contains a single `README.md` describing Cursor IDE capabilities in Chinese.

- **No application code, services, or dependencies** exist in this repo.
- **No build, lint, or test tooling** is configured.
- **No package manager lockfiles or dependency manifests** are present.
- The update script is intentionally a no-op (`echo "No dependencies to install"`).

If application code is added in the future, update this section and the VM update script accordingly.
